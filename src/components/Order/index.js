import { View } from 'react-native';
import styles from './styles';

const Order = () => {

    return (
        <View style={styles.page}>
          
        </View>
      );
}

export default Order;