import { View } from 'react-native';
import styles from './styles';

const MenuItem = () => {

    return (
        <View style={styles.page}>
          
        </View>
      );
}

export default MenuItem;